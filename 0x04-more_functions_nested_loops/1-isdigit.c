#include "main.h"

/**
 * _isdigit - Check for digit character
 * @c: The number to be checked
 * Return: 1 for digit character else 0 
 */

int _isdigit(int c)

{

	if (a >= 48 && a <= 57)
	{
	return (1);
	}
	return (0);
}
