#include "main.h"

/**
 * _isupper - check uppercase character
 * @c: is the char to be checked
 * Return: 1 if lowercase, 0 if not
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
		return (0);
}
