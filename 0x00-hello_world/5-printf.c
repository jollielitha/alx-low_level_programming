#include <stdio.h>
/**
 * main - Entry point
 * Description: The function prints a message to the console using print()
 * Return: always 0 (Success)
 */

int main(void)
{
	printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
